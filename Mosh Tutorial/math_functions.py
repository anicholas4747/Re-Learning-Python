import math 
# you can import modules from the python 3 library or other files on your computer
# make sure not to name a file the name of a module or else python will get confused

print(math.floor(2.9))
print(math.dist([0,0],[2,2])) # euclidean distance, takes two points[] as args
print(math.sqrt(8))